{
	"name": "@cf-monorepo/db",
	"version": "0.0.0",
	"private": true,
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"type-check": "tsc --noEmit",
		"lint": "biome lint ./src",
		"lint:fix": "biome lint --write ./src",
		"generate": "drizzle-kit generate",
		"migrate": "drizzle-kit migrate",
		"studio": "drizzle-kit studio",
		"setup": "pnpm --filter @cf-monorepo/auth generate-schema && pnpm generate",
		"clean": "rm -rf .turbo node_modules"
	},
	"dependencies": {
		"@cf-monorepo/types": "workspace:*",
		"@neondatabase/serverless": "^1.0.2",
		"drizzle-orm": "^0.44.7"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"drizzle-kit": "^0.31.7",
		"typescript": "^5.3.3"
	}
}
