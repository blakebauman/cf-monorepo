{
	"name": "@repo/{{name}}",
	"version": "0.0.0",
	"private": true,
	"description": "{{description}}",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"type-check": "tsc --noEmit",
		"lint": "biome lint ./src",
		"lint:fix": "biome lint --write ./src"{{#if includeTests}},
		"test": "vitest run"{{/if}},
		"clean": "rm -rf .turbo node_modules"
	},
	"dependencies": {
		"@repo/types": "workspace:*"
	}{{#if needsHono}},
	"peerDependencies": {
		"hono": "^4.10.7"
	}{{/if}},
	"devDependencies": {
		"typescript": "^5.3.3"{{#if needsWorkersTypes}},
		"@cloudflare/workers-types": "^4.20240208.0"{{/if}}{{#if needsNodeTypes}},
		"@types/node": "^20.11.0"{{/if}}{{#if includeTests}},
		"vitest": "^3.2.4"{{/if}}
	}
}

